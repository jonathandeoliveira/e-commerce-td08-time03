<section id="customer-info">
  <h2 class="text-center display-4 mb-5">Detalhes da sua conta </h2>
    <div class="d-flex justify-content-center text-center">
        <div class="card text-dark bg-light mb-3" style="width: 20rem;">
          <div class="card-body">
            <h5 class="card-title"><%= Customer.human_attribute_name(:name)%>: <%= @customer.name %></h5>
            <p class="card-text"><%= Customer.human_attribute_name(:email)%>: <%= @customer.email %></p>
            <p class="card-text"><%= Customer.human_attribute_name(:registration_number)%>: <%= @customer.registration_number %></p>
            <p class="card-text"><%= Customer.human_attribute_name(:full_adress)%>: <%= @customer.full_adress %></p>
            <p class="card-text"><%= Customer.human_attribute_name(:balance)%>: <%= @customer.balance %></p>
            <%= link_to 'Comprar crédito', customer_rubi_buy_path(@customer.id), :class => "btn btn-primary" %>
          </div>
        </div>
      </div>
</section>
